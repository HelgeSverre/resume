{
  "name": "resume",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "resume": "./bin/resume.js"
  },
  "type": "module",
  "scripts": {
    "build": "rescript",
    "clean": "rescript clean",
    "test": "npm run build && node --test lib/es6/test/*.mjs test-js/*.test.mjs",
    "start": "node bin/resume.js",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "dependencies": {
    "@rescript/core": "^1.6.1",
    "@rescript/runtime": "^12.3.0",
    "clipboardy": "^5.3.1"
  },
  "devDependencies": {
    "oxfmt": "^0.52.0",
    "rescript": "^12.3.0"
  },
  "engines": {
    "node": ">=22"
  }
}
